.venv
__pycache__
*.pyc
node_modules
.git
.gitignore
.DS_Store
*/__pycache__
*.sqlite3
*.log
# keep lockfiles for dependency installs
.editorconfig
.gitattributes
.github
.gitignore
.gitlab-ci.yml
.idea
.pre-commit-config.yaml
.readthedocs.yml
.travis.yml
venv
.envs/
.env
# uv cache — large and not needed in the image
.uv-cache/
# Exclude validator containers - they build separately
validators/
# Exclude generated documentation
docs_build/
# Exclude test and dev files
tests/
# The production acceptance command uses exactly these source-controlled
# canaries. Keep the rest of the test suite out of the runtime image.
!tests/
tests/*
!tests/assets/
tests/assets/*
!tests/assets/fmu/
tests/assets/fmu/*
!tests/assets/fmu/Feedthrough.fmu
!tests/assets/idf/
tests/assets/idf/*
!tests/assets/idf/window_glazing_template.idf
!tests/assets/portfolio_manager/
tests/assets/portfolio_manager/*
!tests/assets/portfolio_manager/property-report-valid.xml
!tests/assets/pdf/
tests/assets/pdf/*
!tests/assets/pdf/aec-issue-package-clean.pdf
!tests/assets/shacl/
tests/assets/shacl/*
!tests/assets/shacl/example_person_shapes.ttl
!tests/assets/shacl/valid_person.ttl
!tests/assets/schematron/
tests/assets/schematron/*
!tests/assets/schematron/calibration/
tests/assets/schematron/calibration/*
!tests/assets/schematron/calibration/calibration-rules-demo.sch
!tests/assets/schematron/calibration/calibration-certificate-valid.xml
*.md
README*
LICENSE*
# Exclude local data and caches
storage/
# Ship the small curated EnergyPlus weather catalogue used by
# initialize_validibot, while excluding any other developer-local data.
data/*
!data/weather/
!data/weather/**
.parcel-cache/
.mypy_cache/
.pytest_cache/
.ruff_cache/
.coverage
# Exclude SCSS/TS source files (compiled output is already in static/)
validibot/static/src/
# Exclude build artifacts
dist/
build/
*.egg-info/
# Claude / AI config
CLAUDE.md
AGENTS.md
.claude/
.vscode/
# Overlay directory used by optional commercial-package builds. The
# Dockerfile reads these via the ``extras`` named context, not via
# the root build context, so excluding them here prevents the runtime
# stage's ``COPY . /app`` from shipping them into the final image.
_extra_packages/

# Sibling repo mount points, if present. The commercial package sources
# are read via --build-context at build time (see just/gcp/mod.just);
# any copies that land in the repo root — historically named
# ``_validibot-pro/`` or ``_validibot-cloud/``, or plain
# ``validibot-pro/`` / ``validibot-cloud/`` from other tooling — must
# stay out of the image.
_validibot-pro/
_validibot-cloud/
validibot-pro/
validibot-cloud/
validibot-enterprise/
