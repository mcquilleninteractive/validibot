# =============================================================================
# Local Docker Compose build + recipe configuration
# =============================================================================
#
# Copy this file to ``.envs/.local/.build`` on first setup. Two layers read it:
#
#   1. ``docker compose --env-file .envs/.local/.build`` — YAML interpolation
#      of ``${FOO}`` references in the compose files (build args, image
#      tags, etc.). Variables here flow to ``docker build``.
#
# This file holds only build-time and recipe-level knobs such as commercial
# package coordinates. It is not mounted into any
# running container via ``env_file``. Runtime configuration, including x402,
# belongs in ``.django`` — see the x402 section there.
#
# Keep this file private; it is gitignored.
# =============================================================================


# -- 1. Docker build-time vars (Docker Compose self-hosters only) -----------
#
# Only set these if you installed Pro/Enterprise from PyPI for a
# community Docker Compose deployment. For ``local-pro`` and ``local-cloud``
# development we editable-install from the sibling directory, so leave
# these empty.
#
# Set one of:
#   VALIDIBOT_COMMERCIAL_PACKAGE=validibot-pro==0.1.0
#   VALIDIBOT_COMMERCIAL_PACKAGE=validibot-enterprise==0.1.0
#   VALIDIBOT_COMMERCIAL_PACKAGE="https://pypi.validibot.com/packages/validibot_pro-0.1.0-py3-none-any.whl#sha256=<hash>"
#
# Point at a mode-0600 netrc containing the pypi.validibot.com login and
# one-time dashboard key. Compose mounts it only into the package-install
# build step; its contents never become an ARG, environment variable, or layer.
VALIDIBOT_COMMERCIAL_PACKAGE=
VALIDIBOT_COMMERCIAL_NETRC=


# -- 2. x402 payments are Django runtime configuration ---------------------
#
# x402 is a cloud-only runtime feature: only validibot-cloud's Django reads it
# and embedded MCP is a separate authenticated channel. All x402 config lives
# in ``.local/.django`` alongside its consuming process. Nothing x402 is
# authored here in ``.build``. See the x402 section of ``.local/.django``.
